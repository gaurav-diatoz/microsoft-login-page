<!DOCTYPE html>
<html>
<head>
    <title>Sign In</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <style>
        * {
            box-sizing: border-box;
            font-family: "Segoe UI", Arial, sans-serif;
        }

        body {
            margin: 0;
            height: 100vh;
            background: #f5f7fa;
        }

        .container {
            display: flex;
            height: 100vh;
            width: 100%;
        }

        /* LEFT LOGIN PANEL */
        .left-panel {
            width: 35%;
            background: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            border-right: 1px solid #e5e7eb;
        }

        .login-box {
            width: 80%;
            max-width: 400px;
        }

        .login-box h2 {
            font-size: 28px;
            margin-bottom: 20px;
            color: #111827;
        }

        /* Azure AD B2C form container */
        #api {
            margin-top: 10px;
        }

        /* RIGHT BRAND PANEL */
        .right-panel {
            width: 65%;
            background: linear-gradient(135deg, #0a0f2c, #1b2a6b);
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 40px;
        }

        .brand h1 {
            font-size: 48px;
            margin-bottom: 10px;
            letter-spacing: 2px;
        }

        .brand p {
            font-size: 16px;
            opacity: 0.85;
        }

        /* RESPONSIVE */
        @media (max-width: 900px) {
            .container {
                flex-direction: column;
            }
            .left-panel, .right-panel {
                width: 100%;
                height: 50%;
            }
        }
    </style>
</head>
<body>

<div class="container">

    <!-- LEFT LOGIN FORM -->
    <div class="left-panel">
        <div class="login-box">
            <h2>Sign In</h2>

            <!-- Azure AD B2C injects login form here -->
            <div id="api"></div>
        </div>
    </div>

    <!-- RIGHT BRAND SECTION -->
    <div class="right-panel">
        <div class="brand">
            <h1>SOLUM</h1>
            <p>Smart Retail solution unifying ESL, digital signage, AI & IoT technologies</p>
        </div>
    </div>

</div>

</body>
</html>
