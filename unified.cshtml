<!DOCTYPE html>
<html>
<head>
  <title>Sign In</title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <style>
    * {
      box-sizing: border-box;
      font-family: "Segoe UI", Arial, sans-serif;
    }

    html, body {
      margin: 0;
      height: 100%;
      background: #f5f7fa;
    }

    /* ===============================
       MAIN SPLIT LAYOUT (SAFE)
    =============================== */

    .b2c-wrapper {
      display: flex;
      height: 100vh;
      width: 100%;
    }

    /* LEFT LOGIN PANEL */
    .b2c-left {
      width: 35%;
      background: #ffffff;
      display: flex;
      justify-content: center;
      align-items: center;
      border-right: 1px solid #e5e7eb;
    }

    /* IMPORTANT: keep Azure panel layout */
    .unified_container {
      width: 100%;
      max-width: 380px;
      margin: 0 auto;
    }

    .panel {
      border: none !important;
      box-shadow: none !important;
      background: transparent !important;
    }

    .panel-body {
      padding: 0 !important;
    }

    /* RIGHT BRAND PANEL */
    .b2c-right {
      width: 65%;
      background: linear-gradient(135deg, #0a0f2c, #1b2a6b);
      color: white;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 40px;
    }

    .brand h1 {
      font-size: 48px;
      letter-spacing: 2px;
      margin-bottom: 10px;
    }

    .brand p {
      font-size: 15px;
      opacity: 0.85;
    }

    /* ===============================
       AZURE INPUT FIX (SAFE OVERRIDE)
    =============================== */

    #api {
      width: 100% !important;
    }

    #attributeList ul {
      padding: 0;
      margin: 0;
      list-style: none;
    }

    #attributeList ul li {
      margin-bottom: 14px;
      width: 100%;
    }

    #attributeList ul li input {
      width: 100% !important;
      height: 46px !important;
      padding: 0 14px !important;
      border-radius: 8px !important;
      border: 1.5px solid #d1d5db !important;
      font-size: 15px !important;
      background: #fff !important;
    }

    #attributeList ul li input:focus {
      outline: none;
      border-color: #2563eb;
      box-shadow: 0 0 0 1px #2563eb;
    }

    .buttons button,
    .buttons input[type="submit"] {
      width: 100% !important;
      height: 46px !important;
      border-radius: 8px !important;
      background: #2563eb !important;
      color: #fff !important;
      border: none !important;
      font-size: 15px !important;
      font-weight: 600 !important;
    }

    /* Hide Microsoft default headings */
    #api h1,
    #api h2,
    #api h3,
    #api .intro {
      display: none !important;
    }

    /* RESPONSIVE */
    @media (max-width: 900px) {
      .b2c-wrapper {
        flex-direction: column;
      }
      .b2c-left,
      .b2c-right {
        width: 100%;
      }
    }
  </style>
</head>

<body>

<div class="b2c-wrapper">

  <!-- LEFT LOGIN PANEL -->
  <div class="b2c-left">
    <div class="container unified_container">
      <div class="row">
        <div class="col-lg-6">
          <div class="panel panel-default">
            <div class="panel-body">

              <img
                class="companyLogo"
                src="https://gaurav-diatoz.github.io/microsoft-login-page/logo.svg"
                alt="Company Logo"
                style="margin-bottom: 20px;"
              />

              <div id="api" role="main"></div>

            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- RIGHT BRAND PANEL -->
  <div class="b2c-right">
    <div class="brand">
      <h1>SOLUM</h1>
      <p>Smart Retail solution unifying ESL, digital signage, AI & IoT technologies</p>
    </div>
  </div>

</div>

</body>
</html>
